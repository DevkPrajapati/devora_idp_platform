VITE_API_URL=http://localhost:8090
# Set to "false" to run the UI against a backend with AUTH_ENABLED=false.
# Any other value (or omitting it) keeps authentication on.
VITE_AUTH_ENABLED=true
VITE_KEYCLOAK_URL=http://localhost:8080
VITE_KEYCLOAK_REALM=idp
VITE_KEYCLOAK_CLIENT=idp-frontend
